# Capture recovery

Automatic capture recovery requires the stored capture plan and checkpoint to be loaded together. Partial, corrupt, or incompatible state is rejected before execution resumes.

Android service integration and physical-device validation remain incomplete.
