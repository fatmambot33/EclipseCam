# Agent pickup

Agents should select one scoped GitHub issue, assign themselves, create a dedicated branch, and avoid overlapping another active issue. Every pull request must include tests, validation evidence, and a `Closes #<issue>` reference.
