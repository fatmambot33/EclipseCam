# AR overlay release note

The CameraX preview now owns a full-size eclipse trajectory overlay layer. It renders projected contact markers and the trajectory arc from a `FramingAssessment`, reports fit and directional guidance, and explicitly shows an unavailable state when location/orientation inputs are not ready. Deterministic presentation snapshots and a Pixel 7 Pro outdoor validation protocol are included.
